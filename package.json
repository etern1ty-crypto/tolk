{
  "name": "tolk",
  "version": "0.1.0",
  "private": true,
  "description": "Толк. — минималистичный мессенджер. Web MVP + product vault.",
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "preview": "npm --prefix apps/web run preview",
    "web:dev": "npm --prefix apps/web run dev",
    "web:build": "npm --prefix apps/web run build",
    "web:preview": "npm --prefix apps/web run preview -- --host 127.0.0.1 --port 4173"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
