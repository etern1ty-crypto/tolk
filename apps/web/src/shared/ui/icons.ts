/** Single icon language for Tolk UI (lucide-react) */
export const iconProps = {
  /** default stroke — also forced via css `svg.lucide` */
  strokeWidth: 1.75 as const,
  size: {
    sm: 16,
    md: 20,
    lg: 22,
    xl: 24,
  },
} as const;
